# Generated / vendored pricing data — collapse in diffs, exclude from language stats.
# Both are regenerated by scripts/gen_pricing.py (the .txt is the sorted DEFAULT_PRICING
# key snapshot for the D9 coverage gate; the .json is a filtered litellm snapshot).
# Reviewing their line-by-line churn on every refresh adds no signal.
scripts/vendor/supported_model_ids.txt linguist-generated=true
scripts/vendor/litellm_model_prices.json linguist-generated=true
