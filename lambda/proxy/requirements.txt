fastapi>=0.115
uvicorn[standard]>=0.34
boto3>=1.34
# bedrock-mantle transport: boto3 cannot address the OpenAI Responses path, so it is
# called over plain HTTP with botocore-signed SigV4 headers (lambda/proxy/mantle.py).
httpx>=0.27
